name: Revision post

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: instalar dependencias con maven
      run: mvn install -DskipTests=true

    - name: ejecutar pruebas
      run: mvn clean test

    - name: ejecutar pruebas
      run: mvn test -Dtest=net.orangehrmlive.runners.logRunner || true

    - name: ejecutar pruebas 2
      run: mvn test -Dtest=net.orangehrmlive.runners.menuRunner || true